[package]
edition.workspace = true
name = "summer-migration"
rust-version.workspace = true
version.workspace = true

[dependencies]
async-trait = { workspace = true }
sea-orm-migration = { workspace = true, features = [
    "runtime-tokio-rustls",
    "sqlx-postgres",
] }
